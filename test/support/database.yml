test:
  main:
    adapter: sqlite3
    database: ":memory:"
    encoding: utf8
    pool: 5
    timeout: 5000

  main_replica:
    adapter: sqlite3
    database: ":memory:"
    encoding: utf8
    pool: 5
    timeout: 5000
